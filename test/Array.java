package com.test;

public class Array {}

	/*array elements are stored in a sequential /continuous order,
	  sequential storage of array elements means that each element is stored in memory next to the previous one, 
	  making it easy to access individual elements using their index. 
	  Sequential storage also enables efficient traversal of the entire array in a loop. 
	 */
